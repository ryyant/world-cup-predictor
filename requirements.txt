# Runtime dependencies only. For development tools (pytest, jupyter, nbformat)
# install the package with its extras instead:  pip install -e ".[dev]"
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
click>=8.1
